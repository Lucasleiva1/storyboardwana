{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos mínimos para la ventana principal de FrameSync.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "process:default",
    "sql:default",
    "sql:allow-execute",
    "updater:default"
  ]
}
